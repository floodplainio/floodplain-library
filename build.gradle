apply from: 'gradle/dependencies.gradle'

buildscript {
    repositories {
        mavenCentral()
    }

}
allprojects {
    repositories {
        jcenter()
    }
}

subprojects {
	apply plugin: "java"
	apply from: "$rootProject.projectDir/gradle/common.gradle"
}

