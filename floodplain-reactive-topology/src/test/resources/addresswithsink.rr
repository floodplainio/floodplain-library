|>topic('@CountryExample')
	->mongo(name='replication',collection='country')
	
|>topic(name='country')
	->joinWith(into='monkey',
			|>topic(name='city')
			    ->group(key='country_id')
		)
		->set('bliep',[@monkey/])
	->sink('@CountryExample')
