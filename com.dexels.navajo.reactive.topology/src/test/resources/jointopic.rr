|>topic(name='database-ACTIVITY')
	->joinWith(|>topic(name='database-ACTIVITYATTRIBUTE')
			    ->group(key='activityid'), into='aap'
		)
	->filter([activityid] > 21)
	->sink('SINK1','SINK2')
	
	
	// todo reverse join impl
	
|>database(topic='dvd.public.city')
	->group(